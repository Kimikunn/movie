# Pencil .pen 到前端代码开发任务清单

## 使用规则
- [ ] 每完成一个子任务就勾选一次。
- [ ] 父任务的所有子任务都勾选后，父任务视为完成。
- [ ] 每次提交代码时，在提交信息中带上任务编号（例如：`feat: 完成 3.1~3.3`）。

## 任务 1：项目准备
完成条件：1.1~1.4 全部勾选。

- [x] 1.1 创建 `design/` 目录。
- [x] 1.2 将最新 `.pen` 文件放入 `design/`。
- [x] 1.3 确认项目依赖可安装、项目可启动（如 `pnpm install`、`pnpm dev`）。
- [x] 1.4 在同一 workspace 中同时打开前端工程与 `.pen` 文件目录。

## 任务 2：技术栈与工程基线
完成条件：2.1~2.5 全部勾选。

- [x] 2.1 锁定技术栈：`Ionic React + TypeScript + Vite + vite-plugin-pwa + Workbox + @rdlabo/ionic-theme-ios26`。
- [x] 2.2 初始化或校验 Ionic React + TypeScript 工程结构。
- [x] 2.3 接入 PWA 基础能力（Manifest、Service Worker 注册、基础缓存策略）。
- [x] 2.4 接入 `@rdlabo/ionic-theme-ios26` 必要样式与 iOS 动画配置。
- [x] 2.5 建立基础目录规范（如 `src/components`、`src/pages`、`src/theme`、`src/tokens`）。

## 任务 3：设计令牌（Design Tokens）落地
完成条件：3.1~3.4 全部勾选。

- [x] 3.1 从 `.pen` 提取颜色、字体、间距、圆角、阴影。
- [x] 3.2 建立 tokens 文件（CSS Variables 或 TS 常量）。
- [x] 3.3 在全局样式中挂载并验证 token 生效。
- [x] 3.4 输出 token 对照表（设计字段 -> 代码变量）。

## 任务 4：组件拆解与实现
完成条件：4.1~4.5 全部勾选。

- [x] 4.1 生成组件清单（Button、Card、Input、Modal、Table 等）。
- [x] 4.2 实现第一批基础组件（优先高复用组件）。
- [x] 4.3 为每个组件补齐状态：默认、hover、disabled、error。
- [x] 4.4 统一组件 API（props 命名、事件命名、尺寸/变体规范）。
- [x] 4.5 补充组件示例页或 Story（便于回归检查）。

## 任务 5：页面骨架搭建
完成条件：5.1~5.4 全部勾选。

- [x] 5.1 先搭建页面结构（Header、Sidebar、Content、Footer）。
- [x] 5.2 按路由拆分页面并接入导航。
- [x] 5.3 完成首轮布局还原（不追求细节像素级一致）。
- [x] 5.4 处理 iPhone 16 Pro 常见视口与安全区（safe-area）问题。

## 任务 6：视觉对齐与交互还原
完成条件：6.1~6.4 全部勾选。

- [x] 6.1 分区块对齐间距、字号、颜色、边框。
- [x] 6.2 还原关键交互态与过渡效果。
- [x] 6.3 重点校验 iOS 风格一致性（列表、弹层、导航动效）。
- [x] 6.4 对照 `.pen` 做第二轮精修，记录差异项并清零。

## 任务 7：数据接入与状态完善
完成条件：7.1~7.4 全部勾选。

- [ ] 7.1 接入真实接口或 mock 数据源。
- [ ] 7.2 完成加载态、空状态、错误态。
- [ ] 7.3 增加离线读取兜底（IndexedDB/缓存读取）。
- [ ] 7.4 明确无法离线完成的操作并给出用户提示。

## 任务 8：PWA 能力完善（iOS 优先）
完成条件：8.1~8.5 全部勾选。

- [ ] 8.1 校验安装能力与“添加到主屏幕”引导文案。
- [ ] 8.2 完成缓存策略分层（静态资源、接口数据、图片等）。
- [ ] 8.3 校验离线可用页面与离线降级行为。
- [ ] 8.4 校验 iOS 推送前置条件与可行范围（仅主屏安装后可用）。
- [ ] 8.5 明确 iOS 不支持项（如 Background Sync）并落地替代方案。

## 任务 9：测试与验收
完成条件：9.1~9.5 全部勾选。

- [ ] 9.1 桌面端与移动端响应式检查。
- [ ] 9.2 iPhone 16 Pro 真机或等效模拟器专项验证。
- [ ] 9.3 性能与加载检查（首屏、缓存命中、离线恢复）。
- [ ] 9.4 核心流程回归测试（关键页面、关键操作）。
- [ ] 9.5 修复阻塞问题并完成验收清单签字。

## 任务 10：提交与变更追踪
完成条件：10.1~10.4 全部勾选。

- [ ] 10.1 按任务编号分批提交代码。
- [ ] 10.2 记录“设计稿版本 ↔ 代码提交”映射。
- [ ] 10.3 更新开发文档（已完成项、遗留问题、下阶段计划）。
- [ ] 10.4 形成下一轮迭代待办列表。

## 全局进度
- [ ] 所有父任务完成（任务 1~10）。





